on heal:
	if heal reason is satiated:
		cancel event
		heal player by 0.5 + (0.5*{p::false::%player's uuid%::regeneration})
	else:
		set heal amount to heal amount + (heal amount*{p::false::%player's uuid%::regeneration})